[workspace]

[package]
name = "typwire-test"
version = "0.2.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[profile.release]
lto = "fat"
strip = true

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
typwire = { path = ".." }
